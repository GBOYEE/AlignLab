# AlignLab Configuration
# Most settings are optional; defaults are in the code.

# Logging
LOG_LEVEL=INFO

# Data directory (where datasets are stored)
DATA_DIR=./data

# Optional: OpenAI API key if using synthetic generation via LLM
# OPENAI_API_KEY=sk-...
